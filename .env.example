# =========================
# Stamp AI Service - env
# Copy to .env and edit
# =========================

# ---- Host port (uncommon default to avoid 80/8000 clashes) ----
HOST_PORT=18080

# ---- App (inside container; usually keep 8000) ----
APP_PORT=8000
UVICORN_WORKERS=2

# ---- App Info ----
APP_NAME=Stamp AI Service
APP_VERSION=0.6.0

# ---- Data directories (可选：自定义数据根目录，如 /data/stamp-ai) ----
# DATA_ROOT=/data/stamp-ai

# ---- Upload limits ----
MAX_UPLOAD_BYTES=31457280
MAX_IMAGE_PIXELS=60000000

# 允许的文件扩展名（逗号分隔，默认含常见图片格式）
# ALLOWED_EXTENSIONS=.jpg,.jpeg,.png,.bmp,.webp,.tif,.tiff

# ---- 输出访问 URL 前缀（生产环境必须配置为公网域名/IP）----
# 例：https://api.example.com/outputs 或 http://192.168.1.100:18080/outputs
# OUTPUT_URL_PREFIX=http://127.0.0.1:18080/outputs

# ---- File cleanup (uploads/outputs/temp) ----
CLEANUP_ENABLED=true
CLEANUP_INTERVAL_SECONDS=3600
CLEANUP_MAX_AGE_SECONDS=86400

# ---- Logs ----
LOG_RETENTION_DAYS=3

# ---- Resource limits (compose deploy.resources) ----
CPU_LIMIT=2.0
MEM_LIMIT=4G

# ---- Build mirrors (China-friendly defaults in compose) ----
APT_MIRROR=mirrors.aliyun.com
PIP_INDEX_URL=https://pypi.tuna.tsinghua.edu.cn/simple