### 执行启动

```bash
docker compose down
docker compose up -d
```

### 检查是否成功

```bash
docker compose ps
docker logs --tail=30 stamp-ai-service
curl http://127.0.0.1:18080/api/health
```

---

请依次执行上面的命令，然后把以下输出贴给我：

1. `docker compose ps`
2. `docker logs --tail=30 stamp-ai-service`
3. `curl http://127.0.0.1:18080/api/health`

这样我可以判断服务是否真正正常了。



请在项目目录执行
Bash

cd /d/web/self/study/stamp-ai-service

# 1) 看 .env 里写的是什么
grep HOST_PORT .env || echo "no HOST_PORT in .env"

# 2) 看容器实际端口映射
docker compose ps
docker port stamp-ai-service
如果看到类似：

text

8000/tcp -> 0.0.0.0:8000
就证实了当前是 8000→8000。

改成 18080
Bash

# 编辑 .env，改成：
# HOST_PORT=18080

# Windows Git Bash 可直接：
sed -i 's/^HOST_PORT=.*/HOST_PORT=18080/' .env
# 如果没有这行，就追加：
grep -q '^HOST_PORT=' .env || echo 'HOST_PORT=18080' >> .env

cat .env | grep HOST_PORT

# 重建端口映射（必须 down 再 up，只 restart 有时不够）
docker compose down
docker compose up -d

# 再确认
```bash
docker port stamp-ai-service
curl -s http://127.0.0.1:18080/api/health
```











## 重建和部署

```
cd stamp-ai-service
docker compose build --no-cache  # --no-cache to force model download
docker compose up -d
```